[package]
name = "muxy-server-core"
description = "Server domain boundary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
getrandom = "0.3"
libproc = "0.14.11"
log = "0.4"
muxy-core = { path = "../muxy-core" }
muxy-protocol = { path = "../muxy-protocol" }
muxy-pty = { path = "../muxy-pty" }
muxy-terminal = { path = "../muxy-terminal" }
muxy-transport = { path = "../muxy-transport" }
muxy-wire = { path = "../muxy-wire" }
postcard = { version = "1", default-features = false, features = ["use-std"] }
serde = { version = "1", features = ["derive"] }
