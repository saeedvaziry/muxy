# Test Rules

- Never ignore, skip, dismiss, or work around a failing test.
- Treat every test failure as real until its root cause is understood and fixed.
- Do not rerun a failed test merely to obtain a passing result. Investigate it first.
- If a test is flaky, fix the flakiness or report it as a blocker instead of accepting a later pass.
